import 'package:flutter/material.dart';

class ActiveSession extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text("Active Session page here"),
      ),
    );
  }
}
